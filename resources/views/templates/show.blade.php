<article>

    <iframe src="{{ $template->demo_url }}" frameborder="0"></iframe>

    <h2>{{ $template->title }}</h2>

    <h4>
        <del>{{ $template->original_price }}</del> {{ $template->price }}</h4>

    <a href="#">Buy</a>

</article>

